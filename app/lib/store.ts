// import { projectType } from '@/lib/types'
// import {create} from 'zustand'

// type initialStateType = {
   
// }
// export const useProjectStore = create<initialStateType>((set)=>({
//     projects:[],
//     setProjects : (projects)=> set({projects}),

// }))